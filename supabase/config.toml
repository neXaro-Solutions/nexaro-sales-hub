project_id = "nexaro-sales-hub"
# Deliberately public contact endpoint. It verifies a short-lived HMAC challenge,
# validates the payload and invokes a service-only rate-limited transaction.
[functions.nx-public-intake]
verify_jwt = false

# Scheduled calls authenticated inside the function using Vault-backed cron secret.
[functions.nx-email-appointments]
verify_jwt = false
